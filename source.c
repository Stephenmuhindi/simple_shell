#include "prototypes.h"
/**
 * main - where all functions are called
 * Return: 0
 */
int main(void)
{
	/*char *directive[128];*/

		prompt_display();
		/*read_input(directive, sizeof(directive));*/
		/*exec_directive(directive);*/
	return (0);
}
